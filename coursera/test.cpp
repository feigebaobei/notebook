#include <iostream>
using namespace std;

int arrLen (int arr[][]) {
  int all = siezOf(arr);
  int ele = siezOf(arr[0])
  return all / ele
}

int main () {
  int year, month, dayofweek, leep_year;
  char week[7][10] = {"saturday", "Suday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday"};
  while((cin >> days) && days != -1) {
    
  }
  return 0;
}